** In order for this to work pandas needs to be installed

Files in repository:

bash_answers.txt (Q1)

parse_mutations.py (Q2)

db_answers.txt (Q3)

For testing: test_run.py
Which includes a dir of test files (test_files)

To run Q 2.1 and 2.2
python parse_mutations.py --file simple_somatic_mutation.open.BLCA-CN.tsv ##tsv file must be added

To run tests:
pytest -vv -s